# Scripts

This directory contains helper scripts for the `enhance-prompt` skill.

## Planned Scripts

- `prompt_analyzer.py` — Analyze a prompt for missing elements (platform, page type, structure, style, colors, components).
- `design_md_extractor.py` — Extract design system blocks from an existing DESIGN.md for prompt injection.

Add scripts here as the skill evolves.
