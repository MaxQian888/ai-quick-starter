# Scripts

This directory contains helper scripts for the writing-safe-shell-commands skill.

## Planned Scripts

- `command-linter.py` — Parse a shell command and flag risky patterns (recursive delete, unquoted variables, etc.).
- `risk-classifier.py` — Classify a natural-language request into low/medium/high/blocked risk levels.

Add scripts here as the skill evolves.
