# Scripts

This directory contains helper scripts for the postgres-sql-writer skill.

## Planned Scripts

- `validate-sql.py` — Parse and validate PostgreSQL SQL syntax using a lightweight parser.
- `generate-query-recipe.py` — Generate a starter query from natural language requirements and schema hints.

Add scripts here as the skill evolves.
