# Scripts

This directory contains helper scripts for the Ink CLI skill.

- `check_ink_version.js` — Quick Node.js script to detect the local Ink and React versions in a project.
- `scaffold_ink_app.sh` — Bash helper to scaffold a minimal Ink CLI app with ESM and TypeScript defaults.

Run scripts from the project root or copy them into the target repository as needed.
