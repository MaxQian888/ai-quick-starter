# Scripts

This directory contains helper scripts for the `design-md` skill.

## Planned Scripts

- `extract_design_tokens.py` — Parse downloaded Stitch HTML to extract color tokens, typography, and spacing values.
- `validate_design_md.py` — Check a generated `DESIGN.md` against the required section schema.

Add scripts here as the skill evolves.
