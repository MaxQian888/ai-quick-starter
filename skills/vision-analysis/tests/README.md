# Tests

This directory contains test cases for the vision-analysis skill.

## Test Cases

1. **Auto-detection** — Verify image paths/URLs are extracted correctly from user messages.
2. **Mode selection** — Confirm the right mode is chosen based on user intent (OCR vs UI review vs chart data).
3. **Output formatting** — Ensure responses follow the structured format for each mode.
4. **Setup instructions** — Validate environment-specific MCP setup commands are accurate.

Add tests here as the skill evolves.
