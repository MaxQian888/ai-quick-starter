# Tests

This directory contains tests for the `develop-react-flow-projects` skill.

## Test Cases

1. **Version Detection** — Verify correct identification of `@xyflow/react` vs legacy `reactflow`.
2. **State Model Audit** — Verify detection of controlled vs uncontrolled flow patterns.
3. **Custom Node Safety** — Verify recommendations for immutable updates and memoization.

Add test implementations here as the skill evolves.
