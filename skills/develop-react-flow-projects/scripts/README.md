# Scripts

This directory contains helper scripts for the `develop-react-flow-projects` skill.

## Planned Scripts

- `audit_react_flow.py` — Scan a codebase for React Flow usage patterns, version, and common issues.
- `migrate_react_flow.py` — Assist with migration from legacy `reactflow` to `@xyflow/react`.

Add scripts here as the skill evolves.
