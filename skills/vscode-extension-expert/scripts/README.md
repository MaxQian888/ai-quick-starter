# Scripts

This directory contains helper scripts for the vscode-extension-expert skill.

## Planned Scripts

- `validate-message-contract.js` — Check that extension and webview message types are synchronized.
- `check-template-compliance.js` — Verify the project still matches the vscode-extension-quick-starter template invariants.

Add scripts here as the skill evolves.
