# Scripts

This directory contains helper scripts for the `draw-mermaid-diagrams` skill.

## Planned Scripts

- `validate_mermaid.py` — Validate Mermaid syntax for common diagram types.
- `diagram_linter.py` — Check diagram readability rules (edge crossings, label length, consistency).

Add scripts here as the skill evolves.
